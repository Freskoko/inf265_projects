# Project structure

There are some shared functions and constants between the object localization/detection files.
These exists in `constants.py`, `graphing.py` and `shared_utils.py`.

The jupyter notebooks for object detection and object localization are aptly named `Object_detection` and `Object_localization`.

The report is available in `report.pdf`
